def main():
    config = Config().get()
    App(
        HTTPSReader(config["reader]),
        ETLPipeline(
            Parser(),
            Transformation(config["transformation]),
            BigQueryWriter(config["writer])
        )
    ).up()


# Add to env configuration.yml based on data source + import all modules